[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pdfkit>=1.0.0",
    "pyhtml2pdf>=0.1.0",
    "redis[hiredis]>=7.0.1",
    "weasyprint>=68.0",
]
